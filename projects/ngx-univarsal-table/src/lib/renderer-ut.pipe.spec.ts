import { RendererUtPipe } from './renderer-ut.pipe';

describe('RendererUtPipe', () => {
  it('create an instance', () => {
    const pipe = new RendererUtPipe();
    expect(pipe).toBeTruthy();
  });
});
